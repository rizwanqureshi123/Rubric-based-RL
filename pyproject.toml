[project]
name = "rubric-eval-task"
version = "0.1.0"
description = "Rubric-driven evaluation harness for grading ML incident analyses."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.67.0",
]
